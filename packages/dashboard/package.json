{
  "name": "@phalanx/dashboard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'Dashboard build — Next.js setup in W5'",
    "test": "echo 'No tests yet'",
    "typecheck": "echo 'Dashboard typecheck — W5'",
    "clean": "rm -rf .next"
  },
  "dependencies": {
    "@phalanx/core": "workspace:*"
  }
}
